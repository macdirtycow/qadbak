    # Same-origin Webmin proxy — Terminal / embeds work in the panel iframe.
# Optional break-glass Webmin proxy (hybrid + QADBAK_SHOW_WEBMIN_NAV).
# Independent installs omit this — see deploy/nginx-qadbak-port.conf
    location /embed/webmin/ {
        proxy_pass https://127.0.0.1:10000/;
        proxy_ssl_verify off;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_buffering off;
        proxy_read_timeout 86400;
    }
