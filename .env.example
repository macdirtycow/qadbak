# VirtualMin Remote API (server-side only)
# Production on same host as Qadbak: often https://127.0.0.1:10000/... (see docs)
VIRTUALMIN_URL=https://localhost:10000/virtual-server/remote.cgi
VIRTUALMIN_USER=root
VIRTUALMIN_PASS=
# Set to true for local UI development without a VirtualMin server
VIRTUALMIN_MOCK=false
# Public URL of Webmin for login links (hybrid only). Independent mode: leave unset; no /embed/webmin in nginx.
# (same origin as Qadbak — required for Terminal iframe). Direct :10000 only for debugging.
# WEBMIN_UI_URL=https://your-server.example.com/embed/webmin
WEBMIN_UI_URL=https://localhost:10000
# Panel URL users open in the browser (e.g. http://YOUR_IP:11000)
# QADBAK_PANEL_URL=http://your-server.example.com:11000
VIRTUALMIN_UI_URL=https://localhost:10000
# Usermin (often port 20000 on same host)
USERMIN_UI_URL=https://localhost:20000
# Public panel hostname (metadata + links): https://qadbak.com
QADBAK_PUBLIC_HOST=qadbak.com
# Self-signed Webmin on 127.0.0.1: auto-enabled. Force off: VIRTUALMIN_TLS_INSECURE=false
# VIRTUALMIN_TLS_INSECURE=true

# Qadbak session secret (generate: openssl rand -base64 32, min 16 chars)
SESSION_SECRET=change-me-to-a-long-random-string-at-least-16

# Local dev: mock API + cookies on http://localhost (required for npm run start)
VIRTUALMIN_MOCK=true
QADBAK_COOKIE_SECURE=false

# Qadbak bind (for nginx reverse proxy)
PORT=3000

# Native terminal WebSocket (pm2 app qadbak-terminal)
QADBAK_TERMINAL_WS_PORT=3001
QADBAK_TERMINAL_WS_HOST=127.0.0.1

# Hosting backend: virtualmin (default) | mock | native (phase 8, not implemented)
QADBAK_PROVISIONER=virtualmin

# Phase 4: show Webmin module menus in Server admin nav (default: hidden)
# QADBAK_SHOW_WEBMIN_NAV=true
